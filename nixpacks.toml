[phases.setup]
nixPkgs = ["python312", "gcc"]

cmds = ["python -m venv /opt/venv", "cd backend && /opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "cd backend && . /opt/venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
